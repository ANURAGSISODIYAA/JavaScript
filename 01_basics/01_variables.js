// Const variable can be change once defined
const Accountname="Anurag";

/* variables declared by let are only available inside the block
 where they're defined. */
 let AcoountEmail="anurag@gmail.com";

 /* Variables declared by var are available 
 throughout the function in which they're declared. */
 var AccountType="Current";

 //we can define a variable without a type 
 AccountBranch="Indore"

 //iF we dont assign a value it willgive as Undfined answer Althought Const must be initaliseed
 let AccountPassword;